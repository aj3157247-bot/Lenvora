# Setup

1. Clone the repository.
2. Open `android/` in Android Studio.
3. Configure backend environment variables.
4. Configure the admin panel.
5. Add offline models to `models/`/Android assets as they are implemented.
