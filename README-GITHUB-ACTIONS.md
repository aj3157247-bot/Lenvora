# Lenvora V2 - GitHub Actions

Prepared workflows:
- CI: validates repository structure
- Backend: install/build/test
- Admin: install/build/test
- Android: test/build when Gradle project exists
- Deploy: manual placeholder

Put secrets in GitHub Actions Secrets, never directly in source code.
